{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Mgldvd Theme",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}",
                "${workspaceFolder}"
            ]
        }
    ]
}
